<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme, tắt luôn ActionBar/Window title -->
    <style name="Base.Theme.PhuongLink_app" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Tắt tiêu đề của window -->
        <item name="android:windowNoTitle">true</item>
        <!-- Tắt luôn action bar nếu có -->
        <item name="windowActionBar">false</item>
    </style>

    <!-- Theme chính của app -->
    <style name="Theme.PhuongLink_app" parent="Base.Theme.PhuongLink_app">
        <!-- Các tùy chỉnh màu sắc, typography… khác của bạn -->
    </style>

</resources>
